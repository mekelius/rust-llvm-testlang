// RUN: %parse < %s

print_positive_int(x, y, z) {
    if (x >= 0) {
        print_int(x);
    }

    if (y >= 0) {
        print_int(y);
    }

    if (z >= 0) {
        print_int(z);
    }
}

main() {
    // lol unary minus not yet implemented
    print_positive_int(23, 0-12, 0-1);           
}
