// RUN: %testc < %s | clang-22 -x ir - -o %t
// RUN: ! %t 
// | filecheck %s
// This one should fail because mutable variables aren't yet a thing

main() {
    let x = 0;

    // while(x != 9) {    
    //    print_int(8);
    // }
}